Full CRUD, reusable components, local state shared across multiple levels

Next steps, incorporate state management library, convert to typescript (should I do that first? It may be easier now than later?)
